dist/
node_modules/
package-lock.json

# README.md contains the api-version-sync bot-managed block, whose exact byte
# layout is shared verbatim with the other SDK repos (nexus-exchange-rs et al.)
# and is regenerated by scripts/sync_api_version.py. Prettier wants blank lines
# inside the <!-- api-version-sync:start/end --> markers, which the (intentionally
# identical) sync script does not emit — letting prettier reformat it would make
# every bot bump PR fail the format check. Keep README.md out of prettier.
README.md
